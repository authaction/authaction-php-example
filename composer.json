{
    "name": "authaction/authaction-php-example",
    "description": "Plain PHP API example with AuthAction JWKS-based JWT validation",
    "type": "project",
    "require": {
        "php": "^8.2",
        "firebase/php-jwt": "^6.10",
        "vlucas/phpdotenv": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true
    }
}
